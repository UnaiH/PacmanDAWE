this.checkIfHitSomething = function(playerX, playerY, row, col){
			var tileID = {
	    			'door-h' : 20,
				'door-v' : 21,
				'pellet-power' : 3,
				'pellet': 2
			};
			
			// test8
			// Tu código aquí
			// Gestiona la recogida de píldoras
            var value = this.getMapTile(playerX, playerY);
            if(value==tileID.pellet){
                this.setMapTile(playerX,playerY,0);
                Level.pellets=Level.pellets-1;
			}
            if(value==tileID.pellet-power){
                this.setMapTile(playerX,playerY,0);
                Level.pellets=Level.pellets-1;
			}
			// test9
			// Tu código aquí
			// Gestiona las puertas teletransportadoras
			
            if(value==tileID.door-h){
                if(playerX==0){
                    player.x=(Level.lvlWidth-0.5)*TILE_WIDTH, (row-0.5)*TILE_HEIGHT
                }
                else{
                    player.x=(0-0.5)*TILE_WIDTH, (row-0.5)*TILE_HEIGHT
                }
			}
            if(value==tileID.door-v){
                if(playery==0){
                    player.y=(col-0.5)*TILE_WIDTH, (Level.lvlHeight-0.5)*TILE_HEIGHT
                }
                else{
                    player.y=(col-0.5)*TILE_WIDTH, (0-0.5)*TILE_HEIGHT
                }
			}

			// test12
			// Tu código aquí
			// Gestiona la recogida de píldoras de poder
			// (cambia el estado de los fantasmas)
};